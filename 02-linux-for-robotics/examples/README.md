# Приклади 02: Linux для робототехніки

У `examples/` — скрипт читання serial і unit-файл. Перевірте їх на своєму порту або через socat.

Запуск: `pip install -r requirements.txt` (venv репозиторію).
