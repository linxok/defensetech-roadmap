# Приклади 03: Мережі для робототехніки

У `examples/` — UDP-клієнт і MQTT-підписник. Повний міст дивіться в `solution/udp_ws_bridge.py`.

Запуск: `pip install -r requirements.txt` (venv репозиторію).
