# Приклади 04: Python для телеметрії

У `examples/` — FastAPI-сервіс, UDP-сервер і клієнт місій. Повне рішення лабораторної: `solution/main.py`.

Запуск: `pip install -r requirements.txt` (venv репозиторію).
