# Приклади 07: ArduPilot: керування польотом

У `examples/` — спрощений REST API (інʼєкція дрона, без ACK і таймаутів), guided takeoff і RTL. Еталон із тестами: `solution/ardupilot_api.py`.

Запуск: `pip install -r requirements.txt` (venv репозиторію).
