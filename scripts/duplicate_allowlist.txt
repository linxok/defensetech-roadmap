# Винятки для scripts/check_duplicates.py.
# Один рядок — один фрагмент тексту (нормалізований, підрядок).
# Додавайте лише те, що справді мусить повторюватися: цитати
# стандартів, повідомлення компіляторів, офіційні формулювання.
parser = argparse.ArgumentParser(description=__doc__)
