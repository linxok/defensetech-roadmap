# Шаблони 08: PX4: параметри, uORB і логи

Спільні шаблони лежать у `docs/templates.md`, щоб не дублюватися між
модулями. Для цього модуля актуальні:

- Тест, який падає на поганому вході — для валідації імен параметрів
- Async worker — для циклів MAVSDK
