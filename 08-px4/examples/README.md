# Приклади 08: PX4: параметри, uORB і логи

У `examples/` — спрощений CLI параметрів (позиційні `get/set/list`) і звіт по ULog. Еталон CLI: `solution/px4_param.py`.

Запуск: `pip install -r requirements.txt` (venv репозиторію).
