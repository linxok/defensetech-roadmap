# Самоперевірка 05: Сучасний C++

## Питання

1. Чому `std::jthread` переважає `std::thread` у новому коді?
2. Скільки байт CRC покриває у MAVLink v2?
3. Що буде, якщо не викликати `join()` у деструкторі?
4. Навіщо `reserve` перед заповненням векторів?
5. Як перевірити парсер на стійкість до сміття?

## Завдання без підказок

- Написати thread pool за 30 хвилин без підглядання
- Звірити CRC із pymavlink у REPL
- Зібрати з ASan і виправити перше попередження
- Пояснити RAII на прикладі lock_guard
- Заміряти throughput парсера на 1 МБ даних

## Рефлексія

Що лишилося незрозумілим? Де це перевірити в первинному джерелі?
