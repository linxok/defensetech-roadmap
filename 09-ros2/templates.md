# Шаблони 09: ROS2: мости телеметрії

Спільні шаблони лежать у `docs/templates.md`, щоб не дублюватися між
модулями. Для цього модуля актуальні:

- ROS2 node (rclpy) — стартовий вузол
- Тест, який падає на поганому вході — для конвертації
