# Чекліст 09: ROS2: мости телеметрії

Кожен пункт — команда або артефакт з об'єктивним результатом.
Самозвіт «зрозумів» не зараховується.

- [ ] `python checks/check_lab.py --target solution` проходить
- [ ] `colcon build` без помилок
- [ ] `ros2 topic hz /drone/telemetry` показує потік
- [ ] pytest для `bridge_logic` зелений
- [ ] Вузол коректно завершується (destroy_node/shutdown)

Повний прогін: `python scripts/run_lab_checks.py --module 09-ros2`.
