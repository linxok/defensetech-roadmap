apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: drone-telemetry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          env:
            - name: POSTGRES_USER
              value: drone
            - name: POSTGRES_PASSWORD
              value: secret
            - name: POSTGRES_DB
              value: drone
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: drone-telemetry
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
